%h1 Liste des événements

%ul
  - @events.each do |event|
    %li
      = "#{event.date.strftime("%d/%m/%Y")}"
      = link_to event.name, event_path(event.id), method: :get

= button_to "Nouvel événement", new_event_path, method: :get